var cmd = {}

cmd.name = "queue";
cmd.role = "user";
cmd.group = "Utility";
cmd.use = "queue [page]";
cmd.desc = "Displays the current song queue.";

cmd.run = async (bot, msg, args, guild) => {
}

// module.exports = cmd;

